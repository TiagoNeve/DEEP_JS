/**
 *  Introdução ao JavaScript -- TRYBE
 *
 * Aprender a base do Javascript no curso e depois explorar os frameworks
 *
 *  Frameworks -> Jquery / Angular / NodeJs
 *
 * O junior não tem tanta experiência no desenvolvimento e sobre os softSkils
 *
 * O Pleno, consegue ter um domínio melhor das ferramentas e consegue entender
 * as regras de negócio da aplicação, além de ter uma experiência boa na
 * resolução de problemas e maior engajamento na resolução dos problemas
 *
 * O sênior é capaz de se autogerir, consegue ter uma autonomia muito boa
 * e está constantemente ativo na resolução de problemas e regras de negócio
 * do problema. Além de ser uma referência para a equipe de desenvolvedores
 * e tem um amplo domínio das tecnologias que estão sendo executadas.
 */
/**
 * 1 - Variáveis e constantes - TRYBE
 *
 * Variável -> Estrutura capaz de armazenar o valor, essas variavéis tem
 * nome e valor, esse valor pode ser de vários tipos. Esses valores podem
 * ser atualizados.
 * JS utiliza o padrão de nomeação de variáveis CamelCase.
 * 
 * Constantes -> Utilizado para criar um valor em um local da memória, mas 
 * esse valor não pode ser alterado.
 */
var nomeDaHubDaTrybe = "Sem HUB";
console.log(nomeDaHubDaTrybe);

const DURACAO_MAX = "2 meses";
console.log(DURACAO_MAX);
/**
 * 2 - Tipos primitivos
 * Tipo básicos de dados que existe em todas as lingaugens
 * String -> Conjunto de caracteres
 * Number -> Numeros
 * Boolean -> Valor de true ou false.
 * Para verificar qual o tipo do dado, basta chamar a função typeof();
 */
/**
 * Javascript - PARTE 3
 *
 * 1 - Operadores aritméticos :
 * + -> Soma
 * - -> Subtração
 * * -> Multiplicação
 * / -> Divisão
 * ** -> Exponenciação
 * % -> Módulo
 *
 * 2 - Operadores de Comparação :
 * > / >= / < / <= / == / != / === / !==
 */
var notaDoAluno = 95;
var alunoPassouNaProva = notaDoAluno >= 100; // False
/**
 * 3 - Operadores lógicos :
 * && -> AND
 * || -> OR
 */
/**
 * 4 - Estruturas condicionais :
 *
 * if -> Se, será executado quando a condição for verdadeira
 * else -> Será executado quando a condição for falsa.
 */